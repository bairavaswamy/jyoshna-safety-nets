export function generateMetadataContent(city: string, area: any) {
  return {
    title: `Safety Nets in ${area.name}, ${city} | Jyoshna Safety Nets`,
    description: `Professional balcony, child, and pigeon safety nets in ${area.name}, ${city}. Affordable pricing and expert installation.`,
    keywords: `Safety nets in ${area.name}, Balcony safety nets ${area.name}, Child safety nets ${city}`,
  };
}